extends layouts/main

block content
    h1
        a(href="/accounts") Счета
        span.color-gray-light &nbsp;/&nbsp;
        span#header-item-name Новый счет

    form#form-add-account.form(action='')
        .form-message
        input#number(type='text', placeholder='Номер счета')
        input(type='submit', value='Создать счет')

    script.
        app.sections.accounts.add.init();